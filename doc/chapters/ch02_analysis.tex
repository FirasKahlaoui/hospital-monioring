\chapter{Problem Analysis and Preliminary Study}

\section{Problem Formulation}
Modern clinical environments face a critical challenge in monitoring patient physiological state while simultaneously tracking authorized and unauthorized personnel movement in restricted hospital rooms. Traditional systems are often siloed, requiring separate platforms for vitals monitoring and access control. This project addresses the need for an integrated, multi-tier solution that provides:
\begin{enumerate}
    \item \textbf{Continuous Vitals Acquisition:} Reliable sampling of Heart Rate, SpO2, Temperature, and Humidity at the edge.
    \item \textbf{Temporal Isolation:} Ensuring that network-bound cloud uploads do not interfere with time-sensitive physiological sensor sampling.
    \item \textbf{Real-time Clinical Insights:} Moving beyond raw data to provide statistical anomaly detection and short-horizon trend forecasting.
    \item \textbf{Secure Personnel Tracking:} Utilizing browser-based visual inference to identify authorized staff and log unauthorized presence events without dedicated expensive server-side compute.
\end{enumerate}

\section{System Description}

The system is a networked, multi-tier hospital monitoring solution.
At its base sits the \textbf{ESP32 embedded acquisition node}, which occupies the
sensor layer and embedded layer. Its role is to collect physiological and environmental
measurements, apply local signal processing to derive meaningful values (BPM,
SpO\textsubscript{2}), and transmit structured records to a cloud database.

The system follows a multi-tier architectural pattern designed for high availability and low-latency monitoring. At the edge, the \textbf{IoT Layer} (ESP32) performs continuous data acquisition. This telemetry is pushed to the \textbf{Cloud Persistence Layer} (Firebase), where Realtime Database handles live streams and Firestore manages long-term clinical records (users, people, alerts, and detection events). The \textbf{Service Layer} (Node.js/tRPC) provides a type-safe interface for management operations and AI inference, while the \textbf{Application Layer} (React) delivers a high-fidelity monitoring interface for medical staff. The face-recognition pipeline runs directly in the browser using face-api.js and feeds detection events back to the server for audit logging and alerting.

\begin{figure}[H]
    \centering
	\includegraphics[width=\linewidth]{System Context Diagram.png}
    \caption{System context and operational boundaries.}
    \label{fig:system_context}
\end{figure}

\section{Use Cases}

The following operational scenarios define the expected system behaviour.

\subsection*{Embedded Node Use Cases}

\begin{figure}[H]
    \centering
	\includegraphics[width=\linewidth]{Embedded Subsystem Use Cases.png}
    \caption{Embedded Subsystem Use Cases}
    \label{fig:embedded_use_cases}
\end{figure}

\begin{algorithm}[H]
\caption{Sensor Fault Detection Pseudo-code (UC-5)}
\begin{algorithmic}[1]
\STATE \textbf{Input:} $sensor\_data$
\IF{$sensor\_data$ is NaN \textbf{or} $sensor\_data < threshold$}
    \STATE \text{Discard target sample}
    \STATE \text{Skip upload}
\ELSE
    \STATE \text{Upload sample to database}
\ENDIF
\end{algorithmic}
\end{algorithm}


\subsection*{Backend and Dashboard Use Cases}

\begin{figure}[H]
    \centering
	\includegraphics[width=\linewidth]{Backend and Dashboard Use Cases.png}
    \caption{Backend and Dashboard Use Cases}
    \label{fig:backend_dashboard_use_cases}
\end{figure}

\subsection*{AI Subsystem Use Cases}

\begin{figure}[H]
    \centering
	\includegraphics[width=\linewidth]{AI Subsystem Use Cases.png}
    \caption{AI Subsystem Use Cases}
    \label{fig:ai_use_cases}
\end{figure}

\begin{algorithm}[H]
\caption{Anomaly Detection Pseudo-code (UC-11)}
\begin{algorithmic}[1]
\STATE \textbf{Input:} $vitals\_window$
\IF{\textbf{length}($vitals\_window$) $< 5$}
    \RETURN \text{Insufficient data}
\ENDIF
\STATE $mean \gets$ \textbf{average}($vitals\_window$)
\STATE $std\_dev \gets$ \textbf{standard\_deviation}($vitals\_window$)
\STATE $z\_score \gets \frac{\text{latest\_vital} - mean}{std\_dev}$
\IF{$z\_score > threshold\_critical$}
    \RETURN \text{Critical Anomaly}
\ELSIF{$z\_score > threshold\_warning$}
    \RETURN \text{Warning Anomaly}
\ELSE
    \RETURN \text{Stable}
\ENDIF
\end{algorithmic}
\end{algorithm}


\section{Inputs and Outputs}

\subsection*{Embedded Node — Inputs}

\begin{itemize}
	\item \textbf{DHT22 (GPIO~4, one-wire):} digital temperature (°C) and relative
	      humidity (\%).
	\item \textbf{MAX30102 (I\textsuperscript{2}C fast mode):} raw infrared
	      and red photodiode FIFO values, used to compute BPM and SpO\textsubscript{2}.
\end{itemize}

\subsection*{Embedded Node — Outputs}

\begin{itemize}
	\item \textbf{Firebase RTDB — \texttt{/rooms/\{roomId\}}:}
	      JSON records containing \texttt{temperature}, \texttt{humidity}, and
	      server-side \texttt{timestamp}, pushed on significant change.
	\item \textbf{Firebase RTDB — \texttt{/patients/\{patientId\}}:}
	      JSON records containing \texttt{heartRate}, \texttt{spO2}, and
	      server-side \texttt{timestamp}, pushed on significant change.
\end{itemize}

	\noindent\textbf{Identifier alignment.} The dashboard subscribes to
	\texttt{/patients/\{firebaseId\}} for the selected patient. The ESP32 configuration
	therefore uses the patient's Firebase identifier as \texttt{patientId} to ensure
	consistent stream access across subsystems.

\subsection*{Backend, Web Dashboard, and AI — Inputs/Outputs}

\begin{itemize}
	\item \textbf{Backend Inputs:} tRPC procedure calls (JSON), Firebase ID Tokens, and Firestore/RTDB document snapshots.
	\item \textbf{Backend Outputs:} Structured clinical records (Users, People), alert logs, detection events, and SMTP-formatted email alerts when invoked by the dashboard.
	\item \textbf{Web Inputs:} Firebase RTDB telemetry stream, face-api.js descriptor vectors, and tRPC query results.
	\item \textbf{Web Outputs:} Real-time SVG/Canvas visualizations, detection event logs, and automated toast notifications.
	\item \textbf{AI Inputs:} Sliding-window vectors of Heart Rate and SpO2 values (target length 20; minimum 5 required for anomaly detection).
	\item \textbf{AI Outputs:} Rule-based clinical status (stable/warning/critical), anomaly flags, and 5-step trend forecasts.
\end{itemize}

\section{Constraints}

\subsection*{Memory Constraints}

The ESP32 has 520\,KiB of internal SRAM.
The Firebase FreeRTOS task is allocated 8\,192 bytes of stack, which must accommodate
the TLS handshake context, async client buffers, and local variables.
The NVS partition stores up to ten string key-value pairs for device configuration.

\subsection*{Latency Constraints}

The Firebase upload interval is \texttt{FIREBASE\_INTERVAL\_MS = 1\,000\,ms},
providing a near-real-time update rate.
The DHT22 enforces a hardware minimum inter-sample interval of 2\,000\,ms.


\subsection*{Energy Constraints}

The device is assumed to be mains-powered in a fixed hospital room installation.
No active power management (light sleep, deep sleep, or radio duty cycling) is
implemented in the current version.

\subsection*{ESP32 Platform Limitations}

\textbf{Sensor timing sensitivity.}
The MAX30102 heart rate and SpO\textsubscript{2} sensor produces a continuous stream
of readings that must be drained from its internal buffer regularly.
If the program is busy doing something else — such as waiting for a Firebase upload
to complete, which can take several hundred milliseconds or more — the sensor buffer
fills up and new samples are dropped.
This results in the sensor reporting zero or no data, as if no finger were present,
even when one is.

\textbf{Why two cores are necessary.}
The ESP32 contains two independent processing cores (Protocol CPU and Application CPU) that can run tasks simultaneously. This hardware feature is the direct solution to the timing conflict described above. By assigning high-priority sensor acquisition exclusively to Core~1 and network-bound Firebase communication exclusively to Core~0, the system achieves \textbf{temporal isolation}. The sensor continues reading at its required frequency (draining the MAX30102 FIFO every 25\,ms) regardless of how long the TLS handshake or Firebase upload takes. This ensures that no cardiac samples are dropped during network congestion, maintaining the integrity of the clinical data stream. Furthermore, Core~0's Wi-Fi management remains responsive, allowing for background SSID scanning and reconnection without introducing jitter into the physiological measurements.
\subsection*{AI and Cloud Constraints}

The AI layer operates under \textbf{latency constraints} where inference must complete within the dashboard refresh cycle (15\,s refetch for AI insights) and should remain well below the 1\,Hz telemetry cadence. Computation is performed on the service layer to preserve the ESP32's limited SRAM for data acquisition. The anomaly model requires a minimum of 5 data points to compute a stable standard deviation.

\subsection*{Client-Side Compute Constraints}

The face recognition pipeline executes in the browser and loads multiple
pre-trained face-api.js models from a CDN. Model load time depends on bandwidth
and browser caching; inference performance depends on WebGL availability and the
client CPU/GPU. The detection loop is rate-limited to one frame every 600\,ms to
avoid saturating the UI thread on mid-range devices.

\subsection*{Data Model Constraints}

The Firestore schema uses collections \texttt{users}, \texttt{people},
\texttt{detectionEvents}, \texttt{alertLogs}, and \texttt{roomActivityLogs}.
The Realtime Database stores sensor streams under \texttt{/patients/\{patientId\}}
and \texttt{/rooms/\{roomId\}}, and mirrors detection/alert logs under
\texttt{/detectionEvents/\{userId\}} and \texttt{/alertLogs/\{userId\}} for
real-time client subscription. A base64 storage fallback is used for face
photos when Firebase Storage is not available.
